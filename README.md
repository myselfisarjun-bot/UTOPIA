# Project

This repository contains a mobile app built with **Expo + React Native**.

## Workspaces

### `/mobile`
Expo app that integrates **Supabase Auth** and an onboarding flow.

**Quick start**:

```bash
cd mobile
npm install
npm run start
```
